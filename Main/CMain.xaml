<UserControl x:Class="Superete.Main.CMain"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FAFBFC">

    <UserControl.Resources>
        <!-- Modern Button Style for Dashboard Modules -->
        <Style x:Key="ModuleButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="200"/>
            <Setter Property="Margin" Value="12"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E8EBF0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="border" 
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="16"
                                    Padding="0">
                                <Border.Effect>
                                    <DropShadowEffect Color="#1A202C" 
                                                      BlurRadius="16" 
                                                      ShadowDepth="0" 
                                                      Opacity="0.06"/>
                                </Border.Effect>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4A90E2"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#1A202C" 
                                                          BlurRadius="24" 
                                                          ShadowDepth="0" 
                                                          Opacity="0.12"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="-4"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="0"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#1A202C" 
                                                          BlurRadius="12" 
                                                          ShadowDepth="0" 
                                                          Opacity="0.08"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Header Text Style -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#1A202C"/>
        </Style>

        <!-- Module Label Style -->
        <Style x:Key="ModuleLabelStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#2D3748"/>
        </Style>

        <!-- Status Text Style -->
        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#718096"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" 
                Background="White" 
                Padding="48,28"
                BorderBrush="#E8EBF0" 
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="42" Height="42" 
                Background="#4A90E2" 
                CornerRadius="12" 
                Margin="0,0,16,0">
                        <TextBlock Text="BH" 
                       FontFamily="Segoe UI"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="BusinessHub Pro" 
                   Style="{StaticResource HeaderTextStyle}"
                   VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#F0F4F8" 
                CornerRadius="20" 
                Padding="16,8"
                Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="32" Height="32" 
                         Fill="#CBD5E0" 
                         Margin="0,0,10,0"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Bienvenue" 
                               FontFamily="Segoe UI" 
                               FontSize="11" 
                               Foreground="#718096"/>
                                <TextBlock x:Name="Name"
                               Text="Jean Dupont" 
                               FontFamily="Segoe UI" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="#2D3748"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Button Content="⏻" 
                Background="#E53E3E" 
                Foreground="White"
                FontFamily="Segoe UI"
                FontSize="16"
                FontWeight="Bold"
                Width="40"
                Height="40"
                Padding="0"
                BorderThickness="0" Click="Button_Click_6">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Grid>
                                    <Ellipse Fill="{TemplateBinding Background}"/>
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <ScrollViewer 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled" Margin="0,73,0,0" Grid.RowSpan="2">
            <Grid Margin="48,40,48,40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Dashboard Title -->
                <TextBlock Grid.Row="0"
                           Text="Tableau de bord"
                           FontFamily="Segoe UI"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="#1A202C"
                           Margin="0,0,0,32"/>

                <!-- Module Grid -->
                <Grid Grid.Row="1" HorizontalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- First Row -->
                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="8">
                        <Button Style="{StaticResource ModuleButtonStyle}" 
                                Click="Button_Click">
                            <Button.Background>
                                <ImageBrush ImageSource="images/revenu.png" Stretch="Uniform"/>
                            </Button.Background>
                        </Button>
                        <TextBlock Text="Ventes et Revenus" 
                                   Style="{StaticResource ModuleLabelStyle}"
                                   HorizontalAlignment="Center" 
                                   Margin="0,12,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="8">
                        <Button Style="{StaticResource ModuleButtonStyle}" 
                                Click="Button_Click_2">
                            <Button.Background>
                                <ImageBrush ImageSource="images/inventaire.png" Stretch="Uniform"/>
                            </Button.Background>
                        </Button>
                        <TextBlock Text="Inventaire" 
                                   Style="{StaticResource ModuleLabelStyle}"
                                   HorizontalAlignment="Center" 
                                   Margin="0,12,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="2" Margin="8">
                        <Button Style="{StaticResource ModuleButtonStyle}" 
                                Click="Button_Click_4">
                            <Button.Background>
                                <ImageBrush ImageSource="images/customer-review.png" Stretch="Uniform"/>
                            </Button.Background>
                        </Button>
                        <TextBlock Text="Clients" 
                                   Style="{StaticResource ModuleLabelStyle}"
                                   HorizontalAlignment="Center" 
                                   Margin="0,12,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="3" Margin="8">
                        <Button Style="{StaticResource ModuleButtonStyle}" 
                                Click="Button_Click_3">
                            <Button.Background>
                                <ImageBrush ImageSource="images/4866608.png" Stretch="Uniform"/>
                            </Button.Background>
                        </Button>
                        <TextBlock Text="Fournisseurs" 
                                   Style="{StaticResource ModuleLabelStyle}"
                                   HorizontalAlignment="Center" 
                                   Margin="0,12,0,0"/>
                    </StackPanel>

                    <!-- Second Row (Centered) -->
                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="8">
                        <Button Style="{StaticResource ModuleButtonStyle}" 
                                Click="Button_Click_5">
                            <Button.Background>
                                <ImageBrush ImageSource="images/management.png" Stretch="Uniform"/>
                            </Button.Background>
                        </Button>
                        <TextBlock Text="Gestion de Projets" 
                                   Style="{StaticResource ModuleLabelStyle}"
                                   HorizontalAlignment="Center" 
                                   Margin="0,12,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="2" Margin="8">
                        <Button Style="{StaticResource ModuleButtonStyle}" 
                                Click="Button_Click_1">
                            <Button.Background>
                                <ImageBrush ImageSource="images/parametres.png" Stretch="Uniform"/>
                            </Button.Background>
                        </Button>
                        <TextBlock Text="Paramètres Système" 
                                   Style="{StaticResource ModuleLabelStyle}"
                                   HorizontalAlignment="Center" 
                                   Margin="0,12,0,0"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </ScrollViewer>

        <!-- Footer/Status Bar -->
        <Border Grid.Row="2" 
                Background="White" 
                Padding="48,20"
                BorderBrush="#E8EBF0" 
                BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Ellipse Width="10" Height="10" 
                             Fill="#48BB78" 
                             VerticalAlignment="Center" 
                             Margin="0,0,10,0"/>
                    <TextBlock Text="État du système : Tous les systèmes opérationnels" 
                               Style="{StaticResource StatusTextStyle}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Dernière mise à jour : " 
                               Style="{StaticResource StatusTextStyle}"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="Aujourd'hui, 14h30" 
                               Style="{StaticResource StatusTextStyle}"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>