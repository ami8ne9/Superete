<Window x:Class="Superete.Main.ProjectManagment.WPlus"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Détails de l'Opération" Height="738" Width="900" 
        WindowStartupLocation="CenterScreen"
        Background="#F8FAFC"
        ShowInTaskbar="True"
        WindowStyle="None" ResizeMode="NoResize">

    <Window.Resources>
        <!-- Header Style -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- Section Title Style -->
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- Label Style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <!-- Value Style -->
        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.06" ShadowDepth="2" BlurRadius="12"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close Button Style -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#6B7280"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4B5563"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="Background" Value="#E5E7EB"/>
                                <Setter Property="Foreground" Value="#9CA3AF"/>
                                <Setter Property="Cursor" Value="No"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Article Header Style -->
        <Style x:Key="ArticleHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Background" Value="#F9FAFB"/>
        </Style>

        <!-- Article Cell Style -->
        
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Content -->
            <RowDefinition Height="Auto"/>
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Operation Type Icon -->
                <Border Grid.Column="0" Background="#10B981" Width="48" Height="48" CornerRadius="24" Margin="0,0,16,0" Name="IconColor">
                    <TextBlock Text="V" FontSize="20" FontWeight="Bold" Foreground="White" Name="Start"
                              HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>

                <!-- Title and ID -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="OperationType1" Text="Vente #V-2024-001" Style="{StaticResource HeaderStyle}"/>
                    <TextBlock x:Name="OperationSubtitle" Text="Transaction effectuée avec succès" 
                              FontSize="14" Foreground="#6B7280"/>
                </StackPanel>

                <!-- Status Badge -->
            </Grid>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Operation Information -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Informations de l'opération" Style="{StaticResource SectionTitleStyle}"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="24"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="24"/>
                                <ColumnDefinition Width="*" Name="RightSideContainer"/>
                                <ColumnDefinition Width="24"/>
                                <ColumnDefinition Width="*" Name="RRightSideContainer"/>
                            </Grid.ColumnDefinitions>

                            <!-- Column 1 -->
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="ID Opération:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="OperationId" Text="V-2024-001" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Type d'opération:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="OperationType2" Text="Vente" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Date et heure:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="OperationDate" Text="24/09/2024 - 14:30" Style="{StaticResource ValueStyle}"/>
                            </StackPanel>

                            <!-- Column 2 -->
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Prix total:" Name="TotalPriceLabel" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="TotalPrice" Text="€157.45" FontSize="16" FontWeight="Bold" Foreground="#059669" Margin="0,0,0,16"/>

                                <TextBlock Text="Remise appliquée:" Name="RemiseLabel" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="Remise" Text="0.00 DH" Style="{StaticResource ValueStyle}" Foreground="#DC2626"/>

                                <TextBlock Text="Utilisateur:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="UserName" Text="Sophie Laurent" Style="{StaticResource ValueStyle}"/>
                            </StackPanel>

                            <!-- Column 3 -->
                            <StackPanel Grid.Column="4" >
                                <TextBlock Text="Client:" Name="FC" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="FCName" Text="" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Nombre d'articles:" Name="ArticlesCountLabel" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="ArticlesCount" Text="3 articles" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Transaction Type :" Name="TransactionLabel" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="Transaction" Text="Cash" Style="{StaticResource ValueStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="6">
                                <TextBlock Text="Credit Value :" Name="CreditValueLabel" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="CreditValue" Text="0.00 DH" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Type de payment :" Name="PaymentTypeLabel" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="PaymentType" Text="" Style="{StaticResource ValueStyle}"/>
                            </StackPanel>


                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Articles List -->
                <Border Style="{StaticResource CardStyle}" Name="ArticleOperation">
                    <StackPanel>
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Articles dans cette opération" Style="{StaticResource SectionTitleStyle}"/>
                            <TextBlock Grid.Column="1" x:Name="ArticlesTotalValue" Text="Total: €157.45" 
                                      FontSize="14" FontWeight="Bold" Foreground="#059669" VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Articles Table Header -->
                        <Grid Background="#F9FAFB" Margin="0,0,0,1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <!-- Article Name -->
                                <ColumnDefinition Width="*"/>
                                <!-- Quantité -->
                                <ColumnDefinition Width="*"/>
                                <!-- Prix unitaire -->
                                <ColumnDefinition Width="*"/>
                                <!-- Total -->
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Article" Style="{StaticResource ArticleHeaderStyle}"/>
                            <TextBlock Grid.Column="1" Text="Quantité" Style="{StaticResource ArticleHeaderStyle}" TextAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="Prix unitaire" Style="{StaticResource ArticleHeaderStyle}" TextAlignment="Right"/>
                            <TextBlock Grid.Column="3" Text="Total" Style="{StaticResource ArticleHeaderStyle}" TextAlignment="Right"/>
                        </Grid>

                        <!-- Articles List -->
                        <StackPanel x:Name="ArticlesContainer" />


                        <!-- Summary Row -->
                        <Border Background="#F8FAFC" Margin="0,16,0,0" Padding="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Sous-total: €171.45" FontSize="13" Foreground="#6B7280" Name="Total"/>
                                    <TextBlock Text="Remise: -€12.50" FontSize="13" Foreground="#DC2626" Margin="0,4,0,0" Name="Remise2"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1" Text="Total Final: €157.45"  Name="TotalFinal"
                                          FontSize="18" FontWeight="Bold" Foreground="#059669" 
                                          VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Additional Notes -->
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Padding="0,16,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Imprimer Reçu" Background="#3B82F6" Foreground="White" Name="Print"
                        Style="{StaticResource CloseButtonStyle}" Margin="0,0,12,0"/>
                <Button Content="Selectione les Article a Reverse" Background="#3B82F6" Foreground="White"  Name="ReverseAritcles"
Style="{StaticResource CloseButtonStyle}" Margin="0,0,12,0" Click="ReverseAritcles_Click"/>
                <Button Content="Fermer" Style="{StaticResource CloseButtonStyle}" 
                        Click="CloseButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>