<UserControl x:Class="Superete.Main.Settings.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <!-- Modern Color Palette -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#1F2937"/>
        <SolidColorBrush x:Key="HoverBrush" Color="#F3F4F6"/>
        <SolidColorBrush x:Key="ActiveBrush" Color="#EBF4FF"/>

        <!-- Modern Typography -->
        <FontFamily x:Key="ModernFont">Segoe UI, Arial, sans-serif</FontFamily>

        <!-- Navigation Item Style -->
        <Style x:Key="NavigationItemStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="8,2"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontFamily" Value="{StaticResource ModernFont}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" 
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="Icon" Text="{TemplateBinding Tag}" 
                                          FontFamily="Segoe MDL2 Assets" FontSize="16" 
                                          VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource ActiveBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Active Navigation Item Style -->
        <Style x:Key="ActiveNavigationItemStyle" TargetType="Button" BasedOn="{StaticResource NavigationItemStyle}">
            <Setter Property="Background" Value="{StaticResource ActiveBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>

    <!-- Sidebar Content -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Background="{StaticResource SurfaceBrush}" 
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
            </Border.Effect>

            <StackPanel Margin="0,24,0,0">
                <!-- Header -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="←"
        Width="40"
        Height="38"
        FontSize="25"
        Background="Transparent"
        BorderThickness="0"
        FontWeight="Bold"
        HorizontalContentAlignment="Center"
        VerticalContentAlignment="Center"
        Padding="0"
        Margin="-2,0,0,0" Click="Button_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <TextBlock Text="Settings" FontFamily="{StaticResource ModernFont}" 
                                  FontSize="24" FontWeight="Bold" 
                                  Foreground="{StaticResource TextPrimaryBrush}" Padding="5"
                                  />
                </StackPanel>
                <!-- Navigation Items -->
                <Button x:Name="UserManagementBtn" Style="{StaticResource ActiveNavigationItemStyle}" 
                            Tag="&#xE716;" Content="User Management" Click="NavigationItem_Click"/>
                <Button x:Name="ConfigurationBtn" Style="{StaticResource NavigationItemStyle}" 
                            Tag="&#xE713;" Content="Facture Settings" Click="FactureSettings_Click"/>
                <Button x:Name="DatabaseSettingsBtn" Style="{StaticResource NavigationItemStyle}" 
                            Tag="&#xE1C3;" Content="Methode de Paiment" Click="PaimentMehode_Click"/>
                <Button x:Name="SecurityBtn" Style="{StaticResource NavigationItemStyle}" 
                            Tag="&#xE72E;" Content="Security" Click="NavigationItem_Click"/>
                <Button x:Name="NotificationsBtn" Style="{StaticResource NavigationItemStyle}" 
                            Tag="&#xE7EE;" Content="Notifications" Click="NavigationItem_Click"/>
                <Button x:Name="ReportsBtn" Style="{StaticResource NavigationItemStyle}" 
                            Tag="&#xE8F1;" Content="Reports" Click="NavigationItem_Click"/>
                <Button x:Name="SystemLogsBtn" Style="{StaticResource NavigationItemStyle}" 
                            Tag="&#xE946;" Content="System Logs" Click="NavigationItem_Click"/>
                <Button x:Name="PreferencesBtn" Style="{StaticResource NavigationItemStyle}" 
                            Tag="&#xE713;" Content="Preferences" Click="NavigationItem_Click"/>
            </StackPanel>
        </Border>
        <Grid Grid.Column="1" x:Name="ContentGrid" Background="{StaticResource HoverBrush}"/>
    </Grid>

</UserControl>
